#ifndef Menu_hpp
#define Menu_hpp

#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>
using namespace std;
class LogIn {
    
public:
    bool UserLogin();
    bool AuthenticateUsername(string username);
    bool AuthenticatePassword(string username, string password);
};
