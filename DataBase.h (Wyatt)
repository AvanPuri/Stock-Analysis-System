#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>
#include "SignUp.hpp"
using namespace std;

class DataBase {
protected:
    string UserDatabase;
public:
    DataBase();
    DataBase(string username, string password);
    
    void AddUserInfo(string username, string password);
};
